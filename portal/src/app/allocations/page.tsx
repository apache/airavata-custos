// Route stub for the Allocations sidebar entry; the real HPC allocation
// view will replace this once the backend exists.
import { PlaceholderPage } from "../components/PlaceholderPage";

export default function AllocationsPage() {
  return (
    <PlaceholderPage
      title="Allocations"
      description="HPC allocations associated with this portal will appear here."
    />
  );
}
